# game2

a flutter game, 
for building locally run
flutter build web in the root of the project 

for testing and debug run 
flutter run -d chrome , this targets a build for chrome and opens chrome once the debug build runs


